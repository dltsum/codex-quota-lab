{
  "name": "@quotalab/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "quota-lab": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "pnpm --filter @quotalab/contracts build && vitest run",
    "typecheck": "tsc -b tsconfig.json --pretty false"
  },
  "dependencies": {
    "@quotalab/contracts": "workspace:*",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "vitest": "^4.1.11"
  }
}
